// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line
  let fixedValueModified = fixedValue + 1;

  return fixedValueModified;

  // Only change code above this line
}